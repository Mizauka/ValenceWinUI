<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ValenceWinUI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ValenceWinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
        <Style x:Key="SettingsSectionHeaderTextBlockStyle"
               BasedOn="{StaticResource BodyStrongTextBlockStyle}"
               TargetType="TextBlock">
            <Setter Property="Margin" Value="1,30,0,6" />
        </Style>

        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,2,0,2" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="PageHeader"
            MaxWidth="1064"
            Margin="36,24,36,0"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="设置" />

        <ScrollView
            Grid.Row="1"
            Padding="36,0,36,0"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <StackPanel MaxWidth="1064" Spacing="{StaticResource SettingsCardSpacing}">
                <StackPanel.ChildrenTransitions>
                    <RepositionThemeTransition />
                </StackPanel.ChildrenTransitions>

                <!-- 外观 -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="外观" />

                <!-- 应用主题：RadioButton -->
                <Border Style="{StaticResource SettingsCardStyle}">
                    <Grid RowDefinitions="Auto,Auto">
                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="应用主题" />
                        <StackPanel Grid.Row="1" Margin="0,8,0,0" Spacing="8">
                            <RadioButton x:Name="ThemeLight"   Content="浅色"           Tag="Light"   Checked="ThemeRadioButton_Checked" />
                            <RadioButton x:Name="ThemeDark"    Content="深色"           Tag="Dark"    Checked="ThemeRadioButton_Checked" />
                            <RadioButton x:Name="ThemeSystem"  Content="使用系统设置"    Tag="Default" Checked="ThemeRadioButton_Checked" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 关于 -->
                <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="关于" />
                <Border Style="{StaticResource SettingsCardStyle}">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="ValenceWinUI" />
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="Version 0.0.1" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="基于 WinUI 3 构建的应用程序"
                                   Margin="0,4,0,0" />
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
